<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.atguigu.spzx.product.mapper.ProductMapper">

    <select id="findHotProductSkuList" resultType="com.atguigu.spzx.model.entity.product.ProductSku">
        select ps.*
            from product_sku ps
            inner join product p on ps.product_id = p.id
        where p.status = 1 and p.is_deleted = 0 and ps.is_deleted = 0
        order by ps.sale_num desc
        limit 10
    </select>
</mapper>
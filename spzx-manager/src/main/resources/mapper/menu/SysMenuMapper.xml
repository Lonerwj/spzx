<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.atguigu.spzx.manager.mapper.SysMenuMapper">


    <select id="findMenusByUserId" resultType="com.atguigu.spzx.model.entity.system.SysMenu">
        SELECT distinct sm.*
        FROM sys_menu sm
        INNER JOIN sys_role_menu srm ON sm.id = srm.menu_id
        INNER JOIN sys_role_user sru ON sru.role_id = srm.role_id
        where sru.user_id = #{id} and sm.is_deleted = 0 and srm.is_deleted = 0 and sru.is_deleted = 0
    </select>
</mapper>